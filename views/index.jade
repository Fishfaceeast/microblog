extends layout

block content
  h1 欢迎来到Microblog
  p Microblog是一个基于node.js的微博系统
  p
    button(class="btn btn-primary") 登陆
    button(class="btn btn-light")  立即注册
  div(class="row")
    if posts && posts.length
      each post, index in posts
        if index % 3 == 0
          div(class="row")
            div(class="col-4")
              h2 #{post.user + ' 说:'}
              p=post.content
              p=post.time
        else
          div(class="col-4")
              h2 #{post.user + ' 说:'}
              
              p=post.content
              p=post.time
